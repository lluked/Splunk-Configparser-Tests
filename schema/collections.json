{
    "type": "object",
    "properties": {
        "__name__" : {"type": "string"},
        "enforceTypes" : {"type": "string", "enum": ["true", "false"]}
    },
    "patternProperties": {
        "^field\\..*$": {"type": "string", "enum": ["number", "bool", "string", "time"]}
    },
    "required": [
        "enforceTypes"
    ],
    "additionalProperties": false
}